{
  "rules": {
    "pedidos": {
      ".read": "auth != null",
      "$pedidoId": {
        ".write": "(!data.exists() && auth != null && auth.token.firebase.sign_in_provider == 'anonymous' && newData.child('estado').val() == 'Pendiente' && newData.child('juego').isString() && newData.child('id_jugador').isString() && newData.child('fecha').exists()) || (data.exists() && auth != null && auth.token.email == 'REEMPLAZA@TU_CORREO.COM')"
      }
    }
  }
}
